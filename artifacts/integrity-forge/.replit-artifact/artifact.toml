kind = "web"
previewPath = "/"
title = "IntegrityForge"
version = "1.0.0"
id = "artifacts/integrity-forge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20816

[services.development]
run = "pnpm --filter @workspace/integrity-forge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/integrity-forge", "run", "build" ]
publicDir = "artifacts/integrity-forge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20816"
BASE_PATH = "/"
